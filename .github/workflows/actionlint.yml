name: actionlint

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint workflows
        uses: reviewdog/action-actionlint@v1
